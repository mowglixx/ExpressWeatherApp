{{>docHead}}
    <div class="jumbotron">
    <h1>{{page.title}}</h1>
    <p>Welcome {{user.name}}!</p>
    <p>It is currently <strong>{{weather.forecast.today.weather.conditions}}</strong> in <strong>{{weather.location.town}}</strong></p>

    {{!-- PAGE BODY HERE --}}
    <p>{{page.body}}</p>
    </div>
{{>docFoot}}